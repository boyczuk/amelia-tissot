import React from 'react';

const Gallery: React.FC = () => {
    return (
        <div style={{ padding: '20px', textAlign: 'center' }}>
            <h1>Gallery</h1>
            <p>Take a look at our facility, client transformations, and events!</p>
            <h1>Page In Progress</h1>
        </div>
    );
};

export default Gallery;
